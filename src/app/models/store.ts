export class Store {
    constructor(public name:string,
        public branches:string[],
        public logo:string)

    {
    //       this.name=name;
    //       this.branches=branches;
    //       this.logo=logo;
          
    }
}
